import { useState, useEffect } from 'react';

function name(params) {
    const [state, setstate] = useState(initialState) ;
    useEffect(() => {
        effect
        return () => {
            cleanup
        }
    }, [input])
}

export default 

